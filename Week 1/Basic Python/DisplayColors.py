# Initializing list with elements.
color_list = ["Red", "Green", "White", "Black"]
# Printing the first and last elements in list.
print(f'The first and last colors in the list are: {color_list[0]} and {color_list[len(color_list) - 1]} respectively!')
